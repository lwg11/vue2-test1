{
  "name": "vue2-test1",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  },
  "dependencies": { // 依赖库
    "core-js": "^3.8.3", // 核心js库
    "vue": "^2.6.14"
  },
  "devDependencies": { // 开发依赖库
    "@babel/core": "^7.12.16",
    "@babel/eslint-parser": "^7.12.16",
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-eslint": "~5.0.0",
    "@vue/cli-service": "~5.0.0",
    "eslint": "^7.32.0",
    "eslint-plugin-vue": "^8.0.3",
    "vue-template-compiler": "^2.6.14"
  },
  "eslintConfig": { // eslint配置
    "root": true, // 根目录
    "env": { // 环境
      "node": true, // 启用node环境
      "browser": true, // 启用浏览器环境
      "es6": true // 启用es6环境
    },
    "extends": [ // 继承规则
      "plugin:vue/essential", // 包含了一些基础的规则
      "eslint:recommended"  // 推荐规则
    ],
    "parserOptions": { // 解析器选项
      "parser": "@babel/eslint-parser", // 解析器
      "ecmaVersion": 2020, // 解析为es6
      "sourceType": "module" // 解析为模块
    },
    "rules": {
      "no-console": "warn", // 禁止使用console
      "no-debugger": "warn", // 禁止使用debugger
      "vue/script-setup-uses-vars": "error", // 检测script setup使用的变量
      "vue/no-unused-components": "warn", // 检测未使用的组件
      "vue/no-unused-vars": "warn", // 检测未使用的变量
      "vue/multi-word-component-names": "off" // 组件名称多单词
    }
  },
  "browserslist": [ // 浏览器支持列表
    "> 1%", // 全球超过1%的使用率
    "last 2 versions", // 最后两个版本
    "not dead" // 不支持的浏览器
  ]
}